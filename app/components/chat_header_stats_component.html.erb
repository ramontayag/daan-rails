<div class="text-xs text-gray-500">
  <span class="font-medium"><%= number_with_delimiter(chat.total_tokens) %></span> tokens
  <% if chat.estimated_cost_usd > 0 %>
    • <span class="font-medium"><%= chat.formatted_cost %></span>
  <% end %>
</div>
